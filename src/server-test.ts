import { assert } from "https://deno.land/std@0.97.0/testing/asserts.ts";

// seems like these tests would be more integration, not unit

// creates new folder, inits git, and creates commits
// function setupGitRepo

Deno.test("", () => {
});
